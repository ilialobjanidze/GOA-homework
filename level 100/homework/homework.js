// class vs className
// In JSX, you use `className` instead of `class` because `class` is a reserved keyword in JavaScript.

// Self-Closing Tags
// JSX requires elements without children to be self-closed using a slash

// JavaScript In Your JSX In Your JavaScript
// JSX allows embedding JavaScript expressions inside your JSX code using curly braces.

// Curly Braces in JSX
// Curly braces `{}` are used to insert any valid JavaScript expression inside JSX.

// 20 Digits of Pi in JSX
// Demonstrates inserting complex values like long numbers or calculations directly into JSX.

// Variables in JSX
// You can define variables in JavaScript and insert their values into JSX using curly braces.

// Variable Attributes in JSX
// You can dynamically set HTML attributes in JSX using JavaScript variables inside curly braces.

// Event Listeners in JSX
// JSX handles events using camelCase syntax and expects a function as the event handler.

// JSX Conditionals: If Statements That Don't Work
// Traditional if statements cannot be directly used inside JSX return statements.

// JSX Conditionals: If Statements That Do Work
// Conditional logic must be handled outside the JSX return block, or by returning different elements conditionally.

// JSX Conditionals: The Ternary Operator
// A compact syntax that allows you to conditionally render elements inside JSX expressions.

// JSX Conditionals: &&
// A shorthand for rendering elements conditionally when a certain expression evaluates to true.

// .map in JSX
// The `.map()` function is commonly used in JSX to render lists of elements dynamically from arrays.

// Keys
// When rendering lists in JSX, each element should have a unique `key` prop to help React identify elements efficiently.

// React.createElement
// JSX is just syntactic sugar for `React.createElement()`, which React uses under the hood to create elements.

// Review
// A summary of everything covered: using JavaScript inside JSX, conditional rendering, event handling, dynamic lists, and more.
